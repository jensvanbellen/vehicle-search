"""SQLite engine + schema bootstrap.

PLACEHOLDER — the real engine/session/schema lands with the vertical slice (milestone 2),
once the SQLModel tables exist.
"""

from __future__ import annotations


def init_database() -> None:
    """Create the SQLite schema. PLACEHOLDER — implemented in milestone 2."""
    raise NotImplementedError(
        "init_database is implemented in the vertical slice once SQLModel tables exist."
    )
